name: Daily data collection

on:
  schedule:
    - cron: '0 9 * * *'   # 09:00 UTC daily
  workflow_dispatch:        # allow manual runs

jobs:
  collect:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests pandas web3 scikit-learn

      - name: Run pipeline
        run: python pipeline/multi_protocol.py

      - name: Run anomaly detection
        run: python models/anomaly_detector.py

      - name: Run risk classifier
        run: python models/risk_classifier.py

      - name: Run rebalancing advisor
        run: python models/rebalancing_advisor.py

      - name: Commit new data
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git add data/
          git diff --staged --quiet || git commit -m "data: $(date -u '+%Y-%m-%d')"
          git push
